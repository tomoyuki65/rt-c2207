<% provide(:title, "All users") %>
<h1>All users</h1>

<%= will_paginate @users, previous_label: '← Previous', next_label: 'Next →' %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate @users, previous_label: '← Previous', next_label: 'Next →' %>